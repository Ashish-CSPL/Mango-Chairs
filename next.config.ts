/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
 images: {
  domains: [
    'nxadmin.consociate.co.in',
    'res.cloudinary.com',
    'placehold.co',
  ],
},

};

module.exports = nextConfig;
